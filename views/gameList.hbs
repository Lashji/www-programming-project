<div class="card">
    <div class="card-header">

        <h5 class="card-title">{{questionnaire.title}}</h5>

    </div>
    <div class="card-body">
        {{#each questionnaire}}
        <a class="btn btn-primary" href="/game/games/{{this.id}}">{{this.title}}</a>
        {{/each}}

    </div>
</div>